     1→     1→# EGX Financial Scanner — Worklog / Handover Document
     2→     2→
     3→     3→---
     4→     4→Task ID: 1
     5→     5→Agent: Z.ai Code (main orchestrator)
     6→     6→Task: Initial build of the EGX Financial Scanner from the uploaded master engineering specification (EGX Scanner.docx), adapted to the sandbox stack (Next.js 16 + TypeScript + Prisma/SQLite + shadcn/ui instead of Django/MySQL/Flutter — same layered architecture, API-first so any mobile/web client can consume /api/v1).
     7→     7→
     8→     8→Work Log:
     9→     9→- Analyzed the uploaded spec (60 sections): discovery/pattern-detection product, data-integrity rules, period model, normalization, validation, calculation engine, event engine, scanner engine, review workflow, acceptance tests, phased plan.
    10→    10→- Designed Prisma schema (prisma/schema.prisma): Company, FinancialReport (SHA-256 hash unique, versioned, isRestatement), MetricDefinition (EN/AR alias registry), FinancialValue (original label + unit + confidence + validation preserved), CalculatedMetric (formulaVersion + sourcePeriods, valueStatus OK/DATA_UNAVAILABLE/NOT_APPLICABLE/NOT_COMPUTABLE), FinancialEvent (conditions JSON + EN/AR explanations + ruleVersion), Dividend (null dates never inferred), ScannerRule (JSON conditions), ScannerRun, AuditLog, DataSource. `bun run db:push` applied.
    11→    11→- Built financial core (src/lib/financial/): registry.ts (18 raw metrics with Arabic aliases, 22 calculated metrics incl. market-dependent P/B-P/E that always report DATA_UNAVAILABLE), units.ts (EGP thousand/million/billion normalization), periods.ts (period keys "ANNUAL:2024:", "QUARTERLY:2025:Q2"; YoY-only comparability — never Q vs FY), calc.ts (pure functions; sign transitions are events, missing data ≠ zero, zero denominators explicit), validate.ts (balance-sheet identity 1% tolerance, sign/unit/currency checks), events.ts (11 event types incl. FINANCIAL_RECOVERY / DETERIORATION patterns), scanner.ts (AND-rule engine with per-condition explanations), extract.ts (deterministic PDF text extraction via zlib stream inflate + CSV parsing + EN/AR label detection), recompute.ts, scan-service.ts.
    12→    12→- Fixed ROE/ROA bug found during seed verification (stored as ratio instead of ×100 percent).
    13→    13→- Seed (prisma/seed.ts, `bun run db:seed`): 10 clearly-labeled synthetic DEMO companies across Banks/Real Estate/Industrial/Telecom/Healthcare/Food/Investment/Financial Services/Consumer (incl. turnaround story ALFA & ORET, deterioration story FSTL, accelerating-growth NTEL), 7 reports each (FY2022–2024 + Q1/Q2 2024 + Q1/Q2 2025), ~640 validated values, MTXT in EGP-thousands to prove unit normalization, 8 dividend records, 12 preset scanner rules. All rows flagged isDemoData; every report sourceName says "Synthetic demo dataset — not real EGX filings".
    14→    14→- REST API under src/app/api/v1/: companies (paginated+search+sector), company detail/financials/metrics/events/dividends/reports, scanners CRUD + run (persisted runs, availability check — unavailable metrics never silently ignored), dashboard (scanner cards + recent events + dividend calendar + stats), dividends, metrics registry, health; reports upload (multipart → SHA-256 → duplicate 409 → disk storage → NEW_DOWNLOADED), process (type detect → PDF text/CSV extract → label match → normalization → validation → VALIDATED/NEEDS_REVIEW/FAILED, auto-recompute when clean), approve (admin, recomputes metrics+events); review queue + value actions (approve/edit/reject, every correction audit-logged); admin login (in-memory token, passcode env ADMIN_PASSCODE default "egx-admin").
    15→    15→- Frontend single-page app on `/` (only visible route): app-header (sticky, desktop tabs + mobile chip nav, EN/AR toggle with RTL, dark mode, methodology dialog), dashboard (hero stats, 12 scanner discovery cards with counts + top matches, recent events feed, dividend calendar), scanners view (auto-run from dashboard cards, comparison-basis switch FY/QoQ, expandable "Why did this company match?" panel with ✓/✗ per condition incl. DATA_UNAVAILABLE notes, all-companies "why not matched" details), custom scanner builder (metric/event/dividend conditions, operators, save/delete rules), companies (debounced search, sector filter, pagination, snapshot cards), company detail (header + matched-scanner chips FY/Q, key metric cards with tooltips for unavailable data, pure-CSS trend chart, statements tables per statement type with red negatives, growth & ratios matrix with formula versions, events with full explanations, dividends with "not officially announced" handling, reports with per-value source traceability: original label, unit, confidence, extraction method, SHA-256), dividends view (status chips + month grouping), reports & review (admin-gated upload → process → approve pipeline, review queue with edit/approve/reject, uploaded reports list). Sticky footer (min-h-screen flex + mt-auto) with factual-tool disclaimer. i18n dictionary ~100 keys EN/AR; dir=rtl switch.
    16→    16→- Fixed lint issues (setState-in-effect → derived state + remount key for pending scanner rule; rAF pattern for language restore). `bun run lint` clean.
    17→    17→- Agent-browser E2E verification (all passed): dashboard render + correct scanner counts (Turnaround 2 = ALFA+ORET, Deterioration 1 = FSTL, Profit Growth 3 = DRES/GFOOD/NTEL); scanner card → auto-run → why-matched panels showing actual values ("Profit Growth ≥ 30: 30.4%"); company detail (DRES metrics EGP 4.55B revenue / ROE 6.1% / EPS 0.21; event explanations with real values and events@1 version); Arabic RTL toggle (mirrored layout, Arabic labels/explanations); admin login → CSV upload (NEW_DOWNLOADED + sha256) → process (8 extracted · 8 valid · 0 review) → approve (recompute) → new Q3 2025 data visible in ALFA statements and flows into latest-quarter scanner basis; custom scanner run (acceptance test 6 via UI); dark mode; mobile 390×844 responsive; sticky footer pushes naturally; zero page errors in browser console; dev.log clean.
    18→    18→- Backend acceptance tests exercised via API earlier: Test 1 upload valid ✓, Test 2 duplicate 409 ✓, Test 3 invalid balance sheet → NEEDS_REVIEW ✓ (1.56% > 1% tolerance caught), Test 4 LOSS_TO_PROFIT ✓, Test 6 custom multi-condition ✓, Test 7 missing OCF → availability 8/10 not silently zero ✓, Test 8 P/B = DATA_UNAVAILABLE with explanatory note ✓, Test 10 paginated companies ✓.
    19→    19→
    20→    20→Stage Summary:
    21→    21→- The MVP is complete and browser-verified: upload → extraction → normalization → validation → review/approval → metrics → events → scanners → explained results, plus bilingual responsive UI.
    22→    22→- Key decisions: Next.js/Prisma adaptation of the Django spec (documented in code headers); deterministic label-based extraction with NEEDS_REVIEW fallback (no AI-invented numbers); demo data flagged at row level; admin gate is demo-grade (in-memory token) — production should use NextAuth roles.
    23→    23→- Artifacts: prisma/schema.prisma + seed.ts; src/lib/financial/* (8 modules); src/lib/admin-auth.ts, audit.ts, i18n.tsx, client/api.ts; src/app/api/v1/** (17 route files); src/components/egx/* (8 components); page.tsx shell; package.json script `db:seed`.
    24→    24→- Known limitations / next-phase priorities: (1) PDF extraction is minimal (simple text-based PDFs; scanned → NEEDS_REVIEW, no OCR in sandbox); (2) restatement flow schema-ready but no admin UI yet; (3) admin sessions in-memory (reset on server restart); (4) Arabic server-generated event condition details still English (explanations themselves are bilingual); (5) scanner runs not cached (fine at this scale); (6) suggested next features: TTM periods, dividend-calendar month grid, watchlists, scheduled collectors (DataSource adapter interface ready), market-data adapter to light up P/B-P/E, report file download endpoint, unit tests for calc/events engines.
    25→    25→
    26→    26→---
    27→    27→Task ID: 2
    28→    28→Agent: Z.ai Code (cron webDevReview round 2)
    29→    29→Task: Assess project status, QA via agent-browser, then improve styling and add features (watchlist, sector peer comparison, scanner run history, report download, bilingual event conditions).
    30→    30→
    31→    31→Work Log:
    32→    32→- QA sweep: all 15 API endpoints smoke-tested (200s; correct 404s for unknown rule / seeded-report download); browser console clean; dev.log clean. One stale empty route dir (companies/[id]/matches) removed.
    33→    33→- NEW FEATURE — Sector peer comparison: GET /api/v1/companies/[id]/peers computes sector-median ROE / net margin / revenue growth / D/E / net profit for the latest annual period from stored CalculatedMetric rows (never recomputed ad hoc); falls back to all-sectors basis when the sector has <3 peers (basis flagged in response). New "Peers" tab in company detail renders horizontal bar rows (self bar highlighted emerald, negatives red, vertical median marker, "This company: x · above/below median ↑↓" summary).
    34→    34→- NEW FEATURE — Watchlist (client-side preference, zustand+persist in localStorage, zero backend risk): StarButton on company cards + company detail header; "Watchlist only" filter chip in Companies view (with empty-state hint); amber Watchlist strip on Dashboard with quick-jump chips + Manage link.
    35→    35→- NEW FEATURE — Scanner run history: GET /api/v1/scanners/history (last 20 runs w/ rule, basis, count, timestamp); "Recent scanner runs" card in scanners sidebar with time-ago + match counts, 30s refetch + invalidation on every run.
    36→    36→- NEW FEATURE — Report source download: GET /api/v1/reports/[id]/download streams the stored original document with Content-Disposition, MIME by extension and X-Content-SHA256 header; verified end-to-end with a real upload (200 + exact bytes). Seeded reports correctly 404 (no source file). Download button appears in company Reports tab when localFileRef exists.
    37→    37→- i18n COMPLETION — Event condition details are now bilingual: events.ts produces detailAr alongside detail for all 11 event types (rule version bumped to events@2 via EVENTS_RULE_VERSION constant; recompute.ts no longer hardcodes events@1); EventsList + dashboard pick(detail, detailAr). Re-seeded DB to regenerate all events at events@2.
    38→    38→- STYLING POLISH (mandatory): dashboard stat cards with gradient top accent + tinted icon chips + hover lift; scanner discovery cards with count pill + hover ring; event feed items with tone-colored start border (RTL-aware border-s-4); statements table sticky first column (.sticky-col, inset-inline-start + inset shadow divider, hover tint); companies cards now keyboard-accessible div[role=button] (fixes nested <button> hydration error introduced by star buttons) with Enter/Space support.
    39→    39→- BUGS FIXED: (1) filtered-companies memo returned array instead of data object → "Cannot read properties of undefined (reading 'length')" runtime error; (2) nested <button> inside company card → React hydration error; (3) missing valueOf helper in events.ts rewrite broke seed (restored); (4) scan history panel not refreshing after a run (added query invalidation); (5) ruleVersion hardcoded events@1 in recompute.ts.
    40→    40→- Verification: lint clean; browser-verified Peers tab (median markers, self-highlight, above/below median summaries for all 5 metrics), watchlist (star 2 companies → filter → dashboard strip), run history population, AR events with Arabic condition details + events@2 badges, mobile 390px layout (watchlist strip wraps, cards stack), zero console/page errors after fixes.
    41→    41→
    42→    42→Stage Summary:
    43→    43→- App now covers: discovery dashboard, explained scanning (preset+custom+basis switch), peer benchmarking, watchlist, run history, source download, bilingual everything (UI + event explanations + condition details).
    44→    44→- Data state: fresh seed (10 companies, events@2) + one VALIDATED (unapproved) SIGF Q3 2025 upload left in the review pipeline deliberately — demonstrates the "process → needs approval" state in admin view; approving it via UI recomputes metrics.
    45→    45→- Remaining risks/limitations: PDF extraction still minimal (scanned PDFs → NEEDS_REVIEW, no OCR in sandbox); admin sessions in-memory; watchlist is per-browser (no accounts); Arabic server text for sector names/event labels uses DB strings (fully covered), but scanner rule descriptions remain EN-first (labels are bilingual).
    46→    46→- Recommended next phase: (1) TTM period type + period selector on scanner basis; (2) dividend calendar month-grid view; (3) watchlist-backed alerts digest (events among watched companies); (4) restatement/version admin UI (supersedes flow); (5) unit tests for calc/events/validate engines (pure functions — easy wins); (6) market-data adapter interface so P/B, P/E, yield light up when prices exist.
    47→    47→
    48→    48→---
    49→    49→---
    50→    50→Task ID: 1
    51→    51→Agent: Z.ai Code (main orchestrator)
    52→    52→Task: Transform EGX Financial Scanner from demo-data app into real financial-data platform (per master spec). Phase 1: database foundation, registry expansion, demo-data removal, real EGX company registry.
    53→    53→
    54→    54→Work Log:
    55→    55→- Schema (prisma/schema.prisma) updated + db:push applied: FinancialReport gains statementType (CONSOLIDATED|STANDALONE|UNKNOWN), language, fileName, parserVersion, extractionConfidence, errorMessage, uploadedBy/reviewedBy/reviewedBy/approvedBy + timestamps; unique identity now (companyId, periodType, periodLabel, statementType, version) so consolidated+standalone coexist per period. FinancialValue gains manual-correction trail (originalValue, isManuallyCorrected, correctedBy, correctedAt, correctionReason). CalculatedMetric gains statementType (unique includes it — never mix consolidated/standalone). New tables: FinancialValidationResult (per-check structured results), FinancialExtractionLog (pipeline stage logs), AiAnalysis (cached AI output + dataHash for invalidation).
    56→    56→- registry.ts: RawMetricDef gains isPerShare/isCount flags (EPS/DPS/BVPS/share-count values are NEVER multiplied by report unit scale). Added 20+ canonical metrics with EN+AR aliases: PROFIT_BEFORE_TAX, INCOME_TAX, FINANCE_COST, NET_PROFIT_PARENT, TOTAL_EQUITY_AND_LIABILITIES, EQUITY_PARENT, NON_CONTROLLING_INTERESTS, CURRENT_ASSETS, NON_CURRENT_ASSETS, CURRENT_LIABILITIES, NON_CURRENT_LIABILITIES, CASH_AND_EQUIVALENTS, ACCOUNTS_RECEIVABLE, INVENTORY, INVESTMENTS, SHORT_TERM_DEBT, LONG_TERM_DEBT, RETAINED_EARNINGS, SHARES_OUTSTANDING, BOOK_VALUE_PER_SHARE, DIVIDEND_PER_SHARE, NET_CHANGE_IN_CASH. Removed dangerous "total equity and liabilities" alias from TOTAL_LIABILITIES (broke balance identity). New calc metrics: current_ratio, eps_growth, asset_growth, book_value_per_share + isUnitlessMetric() helper.
    57→    57→- validate.ts rewritten: per-share/count values not unit-scaled; structured StructuredCheck[] output (checkName/category/status/severity) for FinancialValidationResult rows; added PBT−Tax≈NetProfit consistency check (1.5% tol), Assets≈TotalEquityAndLiabilities cross-check, ShortTerm+LongTerm≈TotalDebt check, duplicate-figure detection, TOTAL_EQUITY/EQUITY_PARENT/RETAINED_EARNINGS may be legitimately negative (excluded from negative-sign flag).
    58→    58→- seed.ts rewritten: seeds 64 metric definitions + 31 REAL EGX companies (COMI, QNBA, ADIB, ETEL, VODE, TMGH, PHDC, MNHD, AMER, ORHD, SWDY, ORWE, ABUK, SKPC, AMOC, ECSC, EGAL, MOPCO, JUFO, DOMTY, SUGR, EFOOD, CLHO, AMPH, ISPH, RMDT, EAST, CIRA, HRHO, PSFL, RAYA — public metadata only: ticker/name/sector, isDemoData=false, ZERO financial rows) + 14 preset scanner rules + MANUAL_UPLOAD data source.
    59→    59→- DEMO DATA WIPED: deleted 10 demo companies, 110 reports, 1001 values, 3520 calc metrics, 170 events, 9 dividends, 10 prices, storage/reports cleaned. DB now: 31 real companies, 0 financial data.
    60→    60→
    61→    61→Stage Summary:
    62→    62→- Foundation ready: schema supports consolidated/standalone separation, document management fields, correction audit trail, structured validation results, extraction logs, AI-analysis cache.
    63→    63→- Breaking changes for downstream code (agents must address): FinancialReport.statementType required in uniques; CalculatedMetric.statementType must be set on create and filtered on read (prefer CONSOLIDATED, fallback STANDALONE); validateValues() now returns {results, reportStatus, reportNotes, checks}.
    64→    64→- Interface contract for extraction engine (Task 2-a implements, Task 2-b consumes):
    65→    65→  - NEW src/lib/financial/pdf.ts: parsePdf(buffer): Promise<{pages:{pageNumber,text}[], pageCount, looksScanned, engine:"unpdf"|"zlib-fallback", error?}> — unpdf (installed) primary, existing zlib extractor fallback (move extractor code from extract.ts).
    66→    66→  - extract.ts additions: detectLanguage(text)->"EN"|"AR"|"MIXED"|"UNKNOWN"; detectStatementType(text)->"CONSOLIDATED"|"STANDALONE"|"UNKNOWN"; detectPeriod(text)->{periodType,fiscalYear,periodLabel,sub,periodStart?,periodEnd?,confidence}|null (parses EN+AR date phrases, Arabic month names, Arabic-Indic digits); normalizeNumberToken (Arabic-Indic ٠-٩/U+06F0-9, ٬ thousands, ٫ decimal, trailing minus, parentheses negative); extractFromPages(pages, opts) page-aware extraction preserving sourcePage.
    67→    67→- Unresolved: ISIN left null (to be enriched from documents later); GB Corp/Amer etc. naming verifiable by admin; market data (P/B, P/E) stays DATA_UNAVAILABLE until a price feed exists.
    68→    68→
    69→    69→---
    70→    70→Task ID: 2-b
    71→    71→Agent: pipeline-write-path agent
    72→    72→Task: Rebuild the document-ingestion WRITE path — secure upload with versioning, pre-upload analyze endpoint, full structured process pipeline, manual-correction audit trail, approve/reject with metadata, and statement-type-aware recompute (no demo data, deterministic only).
    73→    73→
    74→    74→Work Log:
    75→    75→- calc.ts: added `currentRatio(curAssets, curLiab)` (DATA_UNAVAILABLE on missing input, NOT_APPLICABLE when current liabilities = 0) and `bookValuePerShare(equity, shares)` (DATA_UNAVAILABLE when shares missing, NOT_APPLICABLE when equity missing or shares ≤ 0) — same MetricResult discipline as existing helpers.
    76→    76→- recompute.ts REWRITTEN (statement-type aware, critical): bucket key is now `${statementType}|${periodKey}` with statementType = STANDALONE or CONSOLIDATED (report UNKNOWN → CONSOLIDATED, documented in `bucketStatementType()` which is also exported); consolidated and standalone NEVER share a bucket. Every CalculatedMetric row now carries statementType (schema unique [companyId, code, periodKey, statementType]). Reports fetched ordered by version asc so a higher-version restatement of the same (statementType, period) fully replaces older values. TTM windows are built PER statement type from that type's own consecutive quarters. Events are detected ONLY from the preferred statement type (CONSOLIDATED when any consolidated filing bucket exists, else STANDALONE, else none) — no FinancialEvent unique-key collisions, no mixing. TTM_FLOW_CODES += PROFIT_BEFORE_TAX, INCOME_TAX, FINANCE_COST, NET_PROFIT_PARENT, NET_CHANGE_IN_CASH, DIVIDEND_PER_SHARE; TTM_POINT_CODES += CASH_AND_EQUIVALENTS, CURRENT_ASSETS, NON_CURRENT_ASSETS, CURRENT_LIABILITIES, NON_CURRENT_LIABILITIES, ACCOUNTS_RECEIVABLE, INVENTORY, INVESTMENTS, SHORT_TERM_DEBT, LONG_TERM_DEBT, RETAINED_EARNINGS, EQUITY_PARENT, NON_CONTROLLING_INTERESTS, TOTAL_EQUITY_AND_LIABILITIES, SHARES_OUTSTANDING, BOOK_VALUE_PER_SHARE. New calc metrics wired: current_ratio@1, eps_growth@1, asset_growth@1, book_value_per_share@1 (equity / shares where shares = per-period SHARES_OUTSTANDING raw value if present, else company.sharesOutstanding). eps@1 / p_b@2 / p_e@2 / market_cap@2 now prefer the per-period SHARES_OUTSTANDING raw value over company-level shares. RecomputeResult gained statementTypes[] + preferredStatementType. Kept exports recomputeCompany/recomputeAll; latestPeriodKeys(companyId, opts?: {statementType}) now prefers CONSOLIDATED rows when duplicates exist (auto-preference) or filters to the requested type. NEW export getCompanyCalcMetrics(companyId, opts?: {statementType}) for read-path agents: reads BOTH statement types and returns the preferred set (CONSOLIDATED when any CONSOLIDATED rows exist, else STANDALONE; explicit param filters strictly — rows carry statementType so callers can verify).
    77→    77→- NEW POST /api/v1/reports/analyze (admin): dry-run pre-upload analysis — NO db write, NO storage. Filename sanitization (path.basename, rejects ".."/separators), extension whitelist .pdf/.csv/.txt, 30MB cap, sha256; detectDocumentKind; PDF → parsePdf (pageCount/looksScanned/engine) → full text; TEXT → utf8; runs detectLanguage/detectStatementType/detectPeriod/detectReportUnit + extractFromPages (PDF) or extractFromCsv/extractFromText; responds { ok, detection: { fileName, fileSize, fileHash, documentKind, pageCount, looksScanned, engine, language, statementType, reportUnit, period, candidateCount, candidateCodes, warnings } } with warnings for scanned PDF, no candidates, no period, unsupported kind (optional companyId is read-only company context).
    78→    78→- POST /api/v1/reports UPGRADED: new form fields statementType (default CONSOLIDATED), language (default UNKNOWN), sourceUrl, notes + strict validation of reportType/periodType/statementType/language/fiscalYear/dates; security = basename sanitization + extension whitelist + 30MB cap + %PDF- magic-byte check for .pdf; exact hash duplicate → 409 (unchanged); LOGICAL duplicate (same companyId+periodType+periodLabel+statementType, different hash) → VERSIONING: version = max+1, isRestatement=true, supersedesId=<latest>, audit UPLOAD_REVISION, response { superseded: true, version }; stores fileName (original), localFileRef under storage/reports (hash-named), fileHash/fileSize, uploadedBy "admin", language, statementType, sourceUrl, notes (admin notes + "Uploaded file: …"), status NEW_DOWNLOADED, and creates a FinancialExtractionLog UPLOAD/INFO row (name, size, hash prefix, version, supersedes). GET list: added statementType+language+parserVersion+extractionConfidence+errorMessage+fileName+uploadedBy+supersedesId to mapping and optional ?statementType= filter.
    79→    79→- POST /api/v1/reports/[id]/process REBUILT as PARSE → EXTRACT → NORMALIZE → VALIDATE → FINALIZE with a FinancialExtractionLog row per stage: PDF via parsePdf — looksScanned → NEEDS_REVIEW with exact message "Scanned/image-based PDF — OCR not available in this environment; manual review required", WARN log, summary response; engine unpdf → extractionMethod PDF_TEXT, zlib-fallback → PDF_ZLIB; candidates = extractFromPages(pages) (page-aware sourcePage preserved); TEXT keeps CSV-vs-plaintext behavior (extractFromCsv when ≥3 commas else extractFromText); ZIP_OFFICE/UNKNOWN → NEEDS_REVIEW with the existing helpful messages; zero candidates → NEEDS_REVIEW (never FAILED — reviewer may fix) with ERROR log + structured FinancialValidationResult EXTRACTION/COMPLETENESS row; auto-detection fill-in only when report.language / report.statementType are UNKNOWN (detectLanguage / detectStatementType), each logged INFO; confident detectPeriod (≥0.6) is logged INFO + appended to notes as "Period suggestion: … — admin-confirmed period not overwritten" (NEVER overwrites the admin period); persist: FinancialValue rows (currency + sourcePage + sourceText mapped from candidates + extractionMethod + confidence), delete-then-create FinancialValidationResult rows from validateValues().checks, stage logs (EXTRACT count, NORMALIZE count + unit tally, VALIDATE reportStatus + notes, FINALIZE final status); report update: processingStatus = reportStatus, parserVersion "extract@2", extractionConfidence = mean of candidate confidences (2 decimals, null if none), errorMessage null on success / first reportNote on failure, language, statementType, notes summary; VALIDATED → recomputeCompany; response { ok, status, message?, extractedCount, validCount, needsReviewCount, failedCount, confidence, language, statementType, checks:[{checkName,status,severity}], notes, recompute }. Whole pipeline wrapped in try/catch → status FAILED + errorMessage + ERROR log row + audit PROCESS_ERROR + explicit 500 JSON {ok:false, error} — never silently fails (spec #22).
    80→    80→- GET /api/v1/reports/[id]: full processing detail — report fields incl. language, statementType, parserVersion, extractionConfidence, errorMessage, version/supersedesId, values[] with the complete correction trail (isManuallyCorrected, originalValue, correctedBy/At, correctionReason), validationResults[] and extractionLogs[] asc. DELIBERATE DEVIATION from "admin-gated": this route was already public and is consumed by the company-detail source-traceability dialog (company-detail.tsx fetches it without admin headers) — hard-gating it would break that page. Implemented tiered access instead: admin token → full payload incl. validationResults/extractionLogs/admin fields; no token → report + values only (data already public via /companies/[id]/reports). Flag to orchestrator if hard gating is preferred.
    81→    81→- POST /api/v1/reports/[id]/approve UPGRADED: guard now requires VALIDATED or APPROVED (idempotent re-approve) → 409 INVALID_STATUS otherwise (pending-value 409 kept); stamps processingStatus APPROVED, approvedBy/approvedAt + reviewedBy/reviewedAt = admin/now, clears errorMessage; audits APPROVE_REPORT with statementType + value count; recomputes.
    82→    82→- NEW POST /api/v1/reports/[id]/reject (admin): body {reason} required; only from VALIDATED/NEEDS_REVIEW/FAILED (409 otherwise); sets processingStatus REJECTED, errorMessage = reason, reviewedBy/reviewedAt = admin/now; audit REVIEW_REJECT; returns {ok, status}.
    83→    83→- review/values/[id] EDIT correction trail: originalValue snapshot = existing originalValue ?? current value (set once, never overwritten by later edits), isManuallyCorrected=true, correctedBy "admin", correctedAt now, correctionReason = body.reason (default "Manual correction during review"); normalizedValue now respects isUnitlessMetric (per-share/count values are never unit-scaled on manual edit); validationNotes append + REVIEW_EDIT audit (includes reason) kept; approve/reject behavior kept; report-status refresh + recompute kept, with one safety tweak: a NEEDS_REVIEW report only flips to VALIDATED when no NEEDS_REVIEW AND no FAILED values remain (rejected values keep it in review — use report-level reject instead).
    84→    84→- Verification: `bun x eslint` clean on all owned files (reports/**, review/**, recompute.ts, calc.ts); `tsc --noEmit` — the ONLY errors in my files are the expected unresolved imports of the parallel extract.ts upgrade (extractFromPages, detectLanguage, detectStatementType, detectPeriod); bun runtime smoke check: recompute.ts module loads and exports recomputeCompany/recomputeAll/latestPeriodKeys/getCompanyCalcMetrics/bucketStatementType (UNKNOWN→CONSOLIDATED verified); calc helpers verified (current_ratio 1000/500=2 OK, zero-den NOT_APPLICABLE, missing DATA_UNAVAILABLE; bvps 2M/400k=5 OK, no-equity NOT_APPLICABLE, no-shares DATA_UNAVAILABLE); read-only DB check: 31 companies, 0 reports (demo data stays gone). Did NOT run db:push/db:seed/dev server.
    85→    85→
    86→    86→Stage Summary:
    87→    87→- Endpoints produced/upgraded: POST /api/v1/reports/analyze (dry-run detection), GET+POST /api/v1/reports (list w/ statementType filter+confidence; secure upload w/ magic bytes, 30MB cap, sanitized names, exact-dup 409, logical-dup → versioned restatement w/ supersedesId + UPLOAD extraction log), POST /api/v1/reports/[id]/process (5-stage logged pipeline, structured validation rows, auto-detect fill-in, period suggestion never overwrites admin period, NEVER silently fails), GET /api/v1/reports/[id] (tiered full-detail), POST approve (metadata + guard), POST reject (new), review values approve/edit/reject (full correction trail).
    88→    88→- recompute.ts is now statement-type aware end-to-end and exposes getCompanyCalcMetrics(companyId, {statementType?}) for read-path agents (CONSOLIDATED preferred when present, else STANDALONE) plus latestPeriodKeys optional statementType param; scan-service/companies read paths MUST filter or use the new helper since (code, periodKey) can now exist twice (once per statement type).
    89→    89→- Decisions: UNKNOWN statementType → CONSOLIDATED bucket (documented); zero-candidate processing → NEEDS_REVIEW (reviewer can fix labels manually) rather than FAILED; report-level reject only from VALIDATED/NEEDS_REVIEW/FAILED (approved reports must be superseded, not rejected); GET [id] kept publicly readable in reduced form to avoid breaking company-detail (documented above).
    90→    90→- Limitations / notes for orchestrator: routes import extractFromPages/detectLanguage/detectStatementType/detectPeriod against the Task 2-a contract — tsc will stay red for my two routes until extract.ts lands; eslint is clean (no type-aware rules). process route maps sourceText via metricCode map because validate.ts ValidatableValue doesn't declare sourceText (consider adding it upstream). FinancialValue.statementType continues to hold the statement SECTION (INCOME_STATEMENT/…) — report-level consolidated/standalone lives on FinancialReport + CalculatedMetric only.
    91→    91→
    92→    92→---
    93→    93→Task ID: 2-c
    94→    94→Agent: read-path-ai agent
    95→    95→Task: Make every read path statement-type aware (per-company CONSOLIDATED-preferred, never mixed) + add grounded AI-analysis layer (documents endpoint, AiAnalysis cache, z-ai LLM generation from verified data only).
    96→    96→
    97→    97→Work Log:
    98→    98→- NEW src/lib/financial/statement-pref.ts — the one shared read-path pattern. Pure functions (no db import at module scope, lazily loaded db helpers → file runs standalone under `bun -e`): statementPreference(rows) (CONSOLIDATED if any row is, else STANDALONE), filterByPreference, preferConsolidatedRows (per-company: keeps a company's CONSOLIDATED rows when it has any, else its STANDALONE rows), orderStatementTypes, defaultStatementType (CONSOLIDATED → STANDALONE → whatever exists). DB helpers: fetchCalc(companyId, where?) → { rows, pref, filtered } (both statement types in one query, preference applied), fetchCalcMany(ids, where?) → per-company { prefByCompany, filteredByCompany }, latestCalcPeriodKeys(companyId) (latest annual/quarterly/ttm from the preferred set — replaces recompute.latestPeriodKeys dependency so company detail no longer couples to the parallel-rewritten recompute.ts), availableReportStatementTypes(companyId) (distinct statementTypes among trusted reports), TRUSTED_REPORT_STATUSES = VALIDATED|APPROVED|EXTRACTED (mirrors recompute.ts TRUSTED_STATUSES so read paths expose exactly the data the calc engine computed from). Exported CalcMetricRow type.
    99→    99→- companies/[id]/financials: added optional ?statementType=CONSOLIDATED|STANDALONE (invalid value → 400 VALIDATION); availableStatementTypes computed from trusted reports only; default per spec (CONSOLIDATED if available, else STANDALONE, else whatever exists; null when nothing trusted exists); values now filtered by report.processingStatus in trusted set AND report.statementType = selected (previously only value-level VALID filter — read path now matches the engine's trust boundary); response adds { statementType, availableStatementTypes }, periods/statements shape unchanged (statements still grouped by INCOME_STATEMENT/BALANCE_SHEET/CASH_FLOW/OTHER from FinancialValue.statementType — note the two "statementType" concepts: report basis vs statement kind).
   100→   100→- companies/[id]/metrics: fetchCalc (both types + preference); ?statementType overrides (validated → 400 on bad value; explicitly requested-but-unavailable type returns empty periods/rows gracefully); response adds { statementType, availableStatementTypes } (CONSOLIDATED listed first); rows shape unchanged.
   101→   101→- NEW companies/[id]/documents (public, backs the company-page Documents tab): GET → { documents: [{ id, periodLabel, periodType, fiscalYear, statementType, language, processingStatus, extractionConfidence, parserVersion, version, isRestatement, fileName, fileSize, fileHash, sourceUrl, approvedAt, createdAt, hasFile: !!localFileRef, valueCount }] } ordered createdAt desc. localFileRef NEVER exposed (spec #23) — file access stays behind /api/v1/reports/[id]/download.
   102→   102→- NEW companies/[id]/ai-analysis (the centerpiece): GET → { analysis: { content, createdAt, periodKey, dataHash, model } | null } (latest row). POST → grounded generation: (a) snapshot = company meta + CalculatedMetric rows of the company's preferred basis grouped by periodKey ({periodLabel, periodType, fiscalYear, calculatedMetrics: {code: {value, status, detail?}}}) + raw normalized values (metricCode, normalizedValue, unit) from trusted reports of the SAME basis joined via reportPeriodKey, attached to the latest-8 period buckets (most recent first; labeled via valueNotes as normalized values: plain EGP monetary / EGP-per-share per-share / unscaled counts) + max-8 recent FinancialEvent rows (eventType, EN label, periodLabel, explanationEn, conditions JSON-parsed) + dataQuality (reportsConsidered, reportStatuses counts, validationResults FAILED/WARNING counts, manuallyCorrectedValues, avgExtractionConfidence, parserVersions); report-only buckets are included when calc rows are missing (e.g. recompute not yet run) so verified values still ground the analysis; (b) snapshotJson compact with every float rounded to 2 decimals, dataHash = sha256(snapshotJson); (c) cache: AiAnalysis row with same (companyId, dataHash) → returned with cached:true, NO LLM call; (d) LLM via z-ai-web-dev-sdk (backend-only, route handler): SYSTEM_PROMPT as assistant role (7 strict rules: only numbers in JSON, missing ≠ zero and must be called "not available", cite period labels, never compare incompatible periods, note data-quality issues, fixed markdown sections Overview/Revenue & Growth/Profitability/Balance Sheet & Leverage/Cash Flow/Risks & Unusual Changes/Summary, no buy/sell advice) + user message with snapshotJson and AR/EN instruction from body/query lang; thinking: { type: "disabled" }; 90s timeout via Promise.race-style wrapper; (e) save AiAnalysis { companyId, periodKey: latest annual or null, content, dataHash, inputSummary: snapshotJson truncated to 20k chars, model: "glm" }, audit-logged (AI_ANALYSIS), → { analysis, cached: false }; (f) NO verified data at all → 400 { error: "NO_DATA", message } (never fabricate from an empty company); LLM failure/timeout/empty content → 502 { error: "AI_FAILED", message }.
   103→   103→- Statement-type-aware read adjustments: companies list snapshot now via fetchCalcMany (one query for all listed ids, per-company preference — a consolidated-filing company never shows standalone snapshot numbers); companies/[id] latestPeriods via latestCalcPeriodKeys (preferred set; matchedPresets/marketPrice logic unchanged); peers route fetches both statement types for the peer set and applies preferConsolidatedRows before medians (each peer contributes its preferred basis); scan-service.ts both db.calculatedMetric.findMany calls now fetch statementType in [CONSOLIDATED, STANDALONE] and apply preferConsolidatedRows (first for latest-period-key resolution per company, second for the scan-context metric rows) — dashboard scanner cards / company matched-presets / scanner runs all become statement-aware through this single fix. events routes (companies/[id]/events, /api/v1/events) verified: they read only FinancialEvent (statement-type-agnostic by design, computed once per preferred basis by recompute) — left untouched.
   104→   104→- Verification: bun x eslint on all 12 owned files → CLEAN; bun x tsc --noEmit → zero errors in owned files (remaining project errors are in parallel-agent-owned files: reports/analyze + reports/[id]/process awaiting 2-a/2-b interfaces, plus pre-existing examples/skills/client-api/db.ts items). Fixed two TS pitfalls found by typecheck: Prisma GetFindResult conditional payload types defeat generic inference (preferConsolidatedRows fell back to its constraint) → concrete annotations/Pick types + explicit type args at call sites; Map built from .map entries needs as const/explicit Map<string,string> (inferred Map<{},{}>). bun -e standalone sanity of statement-pref pure functions PASSED (per-company never-mix, defaults, ordering) — module imports without instantiating PrismaClient thanks to lazy db import. Runtime smoke script against the real DB (31 companies, 0 financial data) exercised every query shape I added: fetchCalc/fetchCalcMany where-composition, distinct statementTypes, financials relation filter (report.status+report.statementType), documents _count, financialValidationResult, isManuallyCorrected count, AiAnalysis create/findFirst/delete roundtrip — ALL PASSED with graceful empty structures. No db:push/db:seed/dev-server restart performed (per instructions).
   105→   105→
   106→   106→Stage Summary:
   107→   107→- Read paths now honor the consolidated/standalone boundary: GET companies (snapshot), companies/[id] (latestPeriods), companies/[id]/financials (?statementType + availableStatementTypes), companies/[id]/metrics (?statementType + availableStatementTypes), companies/[id]/peers, and everything flowing through scan-service (dashboard scanner cards, scanner runs, matched presets) all apply per-company CONSOLIDATED-preferred/STANDALONE-fallback and never mix bases within one company. New public GET companies/[id]/documents (full document metadata incl. language/parserVersion/extractionConfidence/version/isRestatement, hasFile boolean, valueCount; no localFileRef). New AI layer: GET companies/[id]/ai-analysis returns latest cached analysis or null; POST regenerates ONLY when the sha256 of the verified-data snapshot changed (dataHash cache), grounds the model exclusively in preferred-basis calculated metrics + raw normalized values from trusted reports + detected events + a data-quality section, hard-fails 400 NO_DATA on empty data and 502 AI_FAILED on LLM failure/timeout/empty output; inputSummary stores the exact prompt snapshot (≤20k chars) for verification.
   108→   108→- AI grounding design: the LLM never sees unverified or mixed-basis data; the snapshot is deterministic (rounded to 2 decimals) so identical DB state ⇒ identical hash ⇒ cache hit; system prompt enforces no-fabrication, missing-metrics-are-"not available", period-label citation, incompatible-period separation, data-quality callouts, fixed markdown section order, no investment advice. periodKey anchors the analysis to the latest annual period.
   109→   109→- Limitations / notes: (1) POST ai-analysis is public (spec flow defines no admin gate) — consider rate-limiting or gating if abused; model string is "glm" and timeout 90s. (2) financials now filters by trusted report statuses — a NEEDS_REVIEW report's stray VALID values no longer appear on public reads (intentional, matches recompute's trust boundary; approval still publishes). (3) availableStatementTypes for financials comes from trusted REPORTS while metrics derives it from existing CALC rows — they can differ transiently before a recompute. (4) Zero-data companies: financials returns statementType null + empty availableStatementTypes; metrics echoes the STANDALONE default with empty structures; ai-analysis POST → 400 NO_DATA; documents → []. (5) AI output language fixed per request (AR via lang:"ar"); no streaming. (6) Statement-type params accept only CONSOLIDATED|STANDALONE (400 otherwise); UNKNOWN-basis reports surface only via defaultStatementType fallback when nothing else exists.
   110→   110→